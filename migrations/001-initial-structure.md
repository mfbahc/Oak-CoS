# 001 Initial Structure

Ensures ignored local directories exist when requested:

- `workspace/`
- `local/`
- `.oak/`
- `.oak/backups/`
- `.oak/qmd/`

This migration does not overwrite existing private files.
