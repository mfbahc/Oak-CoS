# Evening Wrap Routine

Cadence: daily.

Default output: local wrap in `workspace/artifacts/`.

Summarize completed work, open loops, waiting items, and tomorrow's first useful action. Do not send updates by default.

Update `workspace/context/current-actions.md` only for live sign / check / flag / wait items that should survive into the next fresh session. Keep it short.
