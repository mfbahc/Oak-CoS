# QMD Refresh Routine

Cadence: daily or weekly.

Default output: refreshed local search index.

Run `./scripts/qmd-update` or the runtime equivalent. Do not ingest new private folders without approval.
