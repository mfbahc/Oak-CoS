# Connector Health Check Routine

Cadence: weekly.

Default output: local connector status note.

Check which connectors are enabled in local config. Do not call external systems unless the user asks.
