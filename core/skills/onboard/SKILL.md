---
name: oak-onboard
description: Guide first-run Oak setup and local workspace generation.
---

# Oak Onboarding

Ask the runtime question first:

```text
Where do you plan to run Oak?
```

Then collect assistant identity, boundaries, domains, cadence, connector choices, QMD setup preference, and coaching preference.

Generated files must live in ignored local paths:

- `workspace/`
- `local/`
- `.oak/`

Never write private user data into public core files.
